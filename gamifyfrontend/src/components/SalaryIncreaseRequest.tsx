import {Container} from "react-bootstrap";

export const SalaryIncreaseRequest = (props : {own: boolean}) => {
    return (
        <Container>
            <h1> Salary Increase Request</h1>
        </Container>
    );
}